VITE_GOOGLE_CLIENT_ID=
# Only needed for split deploy (Vercel frontend + separate backend).
# Leave blank locally — the Vite dev proxy handles /api.
# In production set it to your backend origin, e.g. https://your-app.onrender.com
VITE_API_URL=
