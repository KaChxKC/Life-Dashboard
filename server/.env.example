PORT=5000
# Comma-separated list of allowed frontend origins (for CORS).
# Local dev (http://localhost:5173) is always allowed automatically.
# For split deploy, set this to your Vercel URL, e.g. https://life-dashboard.vercel.app
CLIENT_URL=
MONGODB_URI=
JWT_SECRET=
GOOGLE_CLIENT_ID=
